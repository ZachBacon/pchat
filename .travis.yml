sudo: false
language: c
cache: apt
compiler: gcc
script:
    - ./autogen.sh --disable-python --disable-perl --disable-dbus
    - make V=1 -j$(nproc)
notifications:
    irc:
        channels: "chat.freenode.net#valhallalinux"
        template: "Build %{build_url} (%{commit} in %{branch}) by %{author}: %{message}"
        on_success: change
matrix:
     fast_finish: true
addons:
    apt:
        packages:
            - automake
            - autoconf
            - imagemagick
            - intltool
            - libcanberra-dev
            - libdbus-glib-1-dev
            - libglib2.0-dev
            - libgtk-3-dev
            - libnotify-dev
            - libpci-dev
            - libproxy-dev
            - libssl-dev
            - libtool
